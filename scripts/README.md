# Scripts

`build_report.py` reads CSV watchlists and writes dashboard report files.

Current version is an MVP. The next step is to replace placeholder scoring with real daily price and volume indicators.
